# model – Prognos

- `predict.py` – `predict_temperature(date)` (anropas från `main.py`)
- Lägg **`model.pkl`** här när modellen är tränad – då laddas den automatiskt

Träningsskript kan läggas som `train.py` i samma mapp.
